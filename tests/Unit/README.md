# Unit tests

Phase 01 establishes the solution and safety baseline only, so no unit test project is required yet.

Future tests in this directory must remain pure user-mode tests and must never communicate with a real kernel driver.

